<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    @Styles.Render("~/Content/Site.css")

</head>
<body>
    <div class="Body-Section-1">
        @*<div class="navbar navbar-inverse navbar-fixed-top">
                <div class="container">
                    <div class="navbar-header">
                        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                        </button>
                        @Html.ActionLink("Trails 4 You", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
                    </div>
                    <div class="navbar-collapse collapse">
                        <ul class="nav navbar-nav">
                            @*
                                @if (User.Identity.IsAuthenticated)
                            {
                                <li>@Html.ActionLink("Trails", "Index", "Trail")</li>
                            }
                            @if (User.Identity.IsAuthenticated)
                            {
                                <li>@Html.ActionLink("Trail Info", "Index", "TrailsInfo")</li>
                            }
                            @if (User.Identity.IsAuthenticated)
                            {
                                <li>@Html.ActionLink("Trail Meetup", "Index", "TrailMeet")</li>
                            }
                            <li>@Html.ActionLink("About", "About", "Home")</li>
                            <li>@Html.ActionLink("Contact", "Contact", "Home")</li>
                        </ul>
                        @Html.Partial("_LoginPartial")
                    </div>
                </div>
            </div>*@
        <div class="col-lg">
            <div class="Background-2">
                <div class="Background-1">
                        @Html.Partial("_LoginPartial")
                    <div class="container body-content">
                        @RenderBody()
                    </div>
                    <div class="Footer-1">
                        <hr />
                        <footer>
                            <p>&copy; @DateTime.Now.Year - Trails 4 You</p>
                        </footer>
                    </div>
                </div>
            </div>
            @Scripts.Render("~/bundles/jquery")
            @Scripts.Render("~/bundles/bootstrap")
            @RenderSection("scripts", required: false)
        </div>
    </div>
</body>
</html>